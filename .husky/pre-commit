#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Resolve node from nvm if not on PATH (common in GUI git clients)
export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && . "$NVM_DIR/nvm.sh"

pnpm exec lint-staged
